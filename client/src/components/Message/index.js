import React, { useState } from "react";
import Row from "./../Row";
import Col from "./../Col";
import API from "./../../utils/API";
const Message = (props) => {
	return <>Message is...{props.message}</>;
};

export default Message;
